Continue the scan:
 root -l -q 'scanPoints_runD.C'  
with various "run*.C" 

plot using draw_Cost.C

git status
git add -A
git commit -m "Describe what changed"
git push


# one command:
git commit -am "Quick update"
git push

